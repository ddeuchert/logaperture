# logaperture
Runtime logging control for the JVM. A Java agent to view and change log levels without a restart, and to catch runaway log volume automatically — across Logback, Log4j 2, JBoss LogManager and JUL.
